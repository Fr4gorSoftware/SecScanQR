<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="3"
        android:entries="@array/pref_search_engine_entries"
        android:entryValues="@array/pref_search_engine_values"
        android:key="pref_search_engine"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_search_engine" />

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_barcode_search_engine_entries"
        android:entryValues="@array/pref_barcode_search_engine_values"
        android:key="pref_barcode_search_engine"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_barcode_search_enigne" />
</PreferenceScreen>